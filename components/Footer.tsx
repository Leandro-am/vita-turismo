import { Text, View } from 'react-native';
import {styles} from "../styles/styles"

export default function Footer() {
  return (
    <View style={styles.footer}>
        <Text>footer placeholder (mudar quando necessario)</Text>
    </View>

  )
}
